#include <stdio.h>
#include <stdlib.h>


struct Queue {
    int front, rear, size;
    int capacity;
    int *data;
};


struct Queue* createQueue(int item) {
    struct Queue* queue = (struct Queue*)malloc(sizeof(struct Queue));
    queue
}

int main() {


}
